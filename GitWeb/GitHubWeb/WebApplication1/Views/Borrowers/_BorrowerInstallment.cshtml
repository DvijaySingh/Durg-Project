
@model Web.Models.ViewModel.BorrowerViewModel

<table class="table table-striped" id="tblinstallment">
    <thead>
        <tr>

            <th>
                Amount
            </th>
            <th>
                Date
            </th>

            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Lstinstallments)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>

                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.ProductID })*@
                    <button id="@item.InstallmentID" class="btn btn-default btnInstallmetedit">Edit</button>
                    @Ajax.ActionLink("Delete", "DeleteInstallMent", "Borrowers", new { InstallmentID = item.InstallmentID, BorrowerID = item.BorrowerID ,@class="btn btn-default"},
                    new AjaxOptions
                    {
                        Confirm = "are you sure to perform the deletion.",
                        HttpMethod = "POST",
                        UpdateTargetId = "lstinstallment",
                        OnSuccess = "RefreshInstallmentDatatable",
                    })
                </td>
            </tr>
        }
    </tbody>
</table>


